import { createContext } from "react";

//creating a context object NOTE: createcontext() takes an ptional argument,which is default value for the context
const notecontext = createContext()      //contextapi holds state or data   
 
export default notecontext